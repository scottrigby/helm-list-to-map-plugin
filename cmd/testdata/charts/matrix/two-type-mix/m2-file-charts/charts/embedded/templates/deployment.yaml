apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-embedded
spec:
  template:
    spec:
      containers:
        - name: app
          image: nginx:latest
          env:
            {{- toYaml .Values.env | nindent 12 }}
