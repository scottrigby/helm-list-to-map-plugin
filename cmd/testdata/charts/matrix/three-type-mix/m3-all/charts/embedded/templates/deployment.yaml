apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-embedded
spec:
  template:
    spec:
      containers:
        - name: app
          image: nginx:latest
      volumes:
        {{- toYaml .Values.volumes | nindent 8 }}
