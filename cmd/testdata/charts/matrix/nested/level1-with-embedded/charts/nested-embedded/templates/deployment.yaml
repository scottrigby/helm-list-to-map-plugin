apiVersion: apps/v1
kind: Deployment
metadata:
  name: nested-embedded
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            {{- toYaml .Values.env | nindent 12 }}
