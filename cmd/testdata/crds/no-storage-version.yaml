# CRD without storage: true marking
# Purpose: Test fallback to first version when no storage version is marked
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: tests.example.com
spec:
  group: example.com
  names:
    kind: Test
    plural: tests
  versions:
    - name: v1beta1
      schema:
        openAPIV3Schema:
          type: object
